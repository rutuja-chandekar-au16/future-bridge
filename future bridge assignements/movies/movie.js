import react from 'react';

const movieList = (props) => {
    return (
        <>
            {props.movie.map(movie, index) => }
                <div>
                    <img src = () ></img>
                </div>

            ))}
        </>
    );

};

export default movieList